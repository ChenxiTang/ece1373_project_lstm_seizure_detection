#include <stdio.h>
#include <vector>
#include "place_holder.h"

using namespace std;

void summer(vector<float> input, int num_input, float sum) {
	sum = 0.0f;

	for (int i = 0; i < num_inputs; i++)
		sum += input[i];
}